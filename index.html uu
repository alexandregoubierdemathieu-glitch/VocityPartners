<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>VELOCITY — Chief of Staff digital asynchrone</title>
  <meta name="description" content="Décisions plus rapides. Deals closés plus vite. ROI x10. Velocity = Chief of Staff digital asynchrone : 2 demandes/jour, livrables <24h (souvent 1h), 5 000 €/mois, résiliable à tout moment." />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Outfit:wght@500;600;700;800&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #0b0d12;
      --bg-soft: #0f1219;
      --text: #e8ecf3;
      --muted: #9aa4b2;
      --brand: #7dd3fc; /* cyan-300 */
      --brand-strong: #38bdf8; /* cyan-400 */
      --ok: #34d399; /* green-400 */
      --card: #0f131b;
      --stroke: rgba(255,255,255,0.08);
      --shadow: 0 20px 50px rgba(0,0,0,0.35);
      --radius: 20px;
    }
    * { box-sizing: border-box; }
    html, body { height: 100%; }
    body {
      margin: 0;
      font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji";
      background: radial-gradient(1200px 600px at 70% -10%, rgba(56,189,248,0.08), transparent 60%),
                  radial-gradient(900px 500px at 10% 0%, rgba(52,211,153,0.06), transparent 60%),
                  var(--bg);
      color: var(--text);
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }
    a { color: inherit; text-decoration: none; }
    .container { width: 100%; max-width: 1200px; margin: 0 auto; padding: 0 20px; }
    header {
      position: sticky; top: 0; z-index: 40;
      background: linear-gradient(to bottom, rgba(11,13,18,0.9), rgba(11,13,18,0.6));
      backdrop-filter: saturate(140%) blur(8px);
      border-bottom: 1px solid var(--stroke);
    }
    .nav { display: flex; align-items: center; justify-content: space-between; height: 72px; }
    .brand { display: flex; align-items: center; gap: 12px; font-weight: 800; letter-spacing: 0.5px; }
    .logo {
      width: 36px; height: 36px; border-radius: 10px;
      background: conic-gradient(from 200deg, var(--brand-strong), #22d3ee, #a7f3d0, var(--brand-strong));
      box-shadow: 0 10px 30px rgba(56,189,248,0.35);
    }
    .brand span { font-family: Outfit, Inter, sans-serif; font-size: 18px; }
    .nav a { margin: 0 10px; color: var(--muted); font-weight: 500; }
    .btn { display:inline-flex; align-items:center; justify-content:center; gap:10px; padding:14px 18px; border-radius: 14px; border:1px solid var(--stroke); background: #0e1219; color: var(--text); font-weight: 600; }
    .btn:hover { border-color: rgba(125,211,252,0.5); box-shadow: 0 10px 30px rgba(56,189,248,0.15); }
    .btn.primary { background: linear-gradient(180deg, var(--brand), var(--brand-strong)); color: #00121a; border: none; }
    .btn.primary:hover { filter: brightness(1.03); box-shadow: 0 14px 40px rgba(56,189,248,0.35); }

    /* Hero */
    .hero { padding: 80px 0 40px; }
    .headline { font-family: Outfit, Inter, sans-serif; font-weight:800; font-size: clamp(34px, 6vw, 64px); line-height: 1.05; letter-spacing: -0.02em; }
    .headline span { background: linear-gradient(90deg, var(--brand-strong), #a7f3d0); -webkit-background-clip: text; background-clip: text; color: transparent; }
    .subhead { margin-top: 16px; color: var(--muted); font-size: clamp(16px, 2.2vw, 20px); max-width: 820px; }
    .cta-row { margin-top: 28px; display:flex; gap:12px; flex-wrap: wrap; }
    .trust { display:flex; gap:24px; align-items:center; margin-top: 28px; color: var(--muted); font-size:14px; }

    /* Stats strip */
    .stats { margin: 50px 0; display:grid; grid-template-columns: repeat(3, 1fr); gap:14px; }
    .stat { background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.0)); border: 1px solid var(--stroke); border-radius: var(--radius); padding: 18px 20px; }
    .stat h4 { margin:0; font-size: 28px; font-weight:800; font-family: Outfit, Inter, sans-serif; }
    .stat p { margin:6px 0 0; color: var(--muted); font-size:14px; }

    /* Sections */
    section { padding: 50px 0; }
    .grid { display:grid; gap:20px; }

    /* Cards */
    .card { background: linear-gradient(180deg, rgba(255,255,255,0.03), rgba(255,255,255,0.01)); border:1px solid var(--stroke); border-radius: var(--radius); padding: 22px; box-shadow: var(--shadow); }
    .card h3 { margin: 0 0 10px; font-family: Outfit, Inter, sans-serif; font-size: 20px; }
    .mono { font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace; font-weight: 600; color: var(--brand-strong); }

    /* How it works */
    .how .grid { grid-template-columns: repeat(4, minmax(0, 1fr)); }
    .how .card p { color: var(--muted); }
    .badge { display:inline-block; padding:6px 10px; border-radius: 999px; border:1px solid var(--stroke); font-size:12px; color: var(--muted); }

    /* Proof */
    .proof .grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .case h4 { margin:0; font-size: 16px; color: var(--muted); letter-spacing: 0.02em; }
    .kpi { display:flex; gap:16px; margin-top:10px; flex-wrap: wrap; }
    .kpi .pill { padding:8px 12px; border-radius: 12px; background: rgba(125,211,252,0.08); border:1px solid rgba(125,211,252,0.25); font-weight:600; font-size:14px; }

    /* Pricing */
    .pricing { display:grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap:20px; }
    .price { font-family: Outfit, Inter, sans-serif; font-size: 42px; margin: 12px 0; }
    .check { color: var(--ok); margin-right:8px; }
    .muted { color: var(--muted); }

    /* Steps */
    .steps { display:grid; grid-template-columns: repeat(4, minmax(0,1fr)); gap:20px; }
    .step-num { width: 34px; height: 34px; display:flex; align-items:center; justify-content:center; border-radius: 10px; background: rgba(125,211,252,0.15); border:1px solid rgba(125,211,252,0.35); font-weight:700; color:#bff0ff; }

    /* Footer */
    footer { border-top:1px solid var(--stroke); color: var(--muted); padding: 26px 0 60px; }

    /* Responsive */
    @media (max-width: 1024px) {
      .how .grid { grid-template-columns: repeat(2, 1fr); }
      .proof .grid { grid-template-columns: 1fr; }
      .pricing { grid-template-columns: 1fr; }
      .steps { grid-template-columns: 1fr 1fr; }
      .stats { grid-template-columns: 1fr 1fr 1fr; }
    }
    @media (max-width: 640px) {
      .nav a { display:none; }
      .stats { grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>
  <header>
    <div class="container nav">
      <div class="brand">
        <div class="logo" aria-hidden="true"></div>
        <span>VELOCITY</span>
      </div>
      <nav>
        <a href="#offre">Offre</a>
        <a href="#preuves">Preuves</a>
        <a href="#pricing">Tarif</a>
        <a href="#onboarding">Onboarding</a>
        <a class="btn primary" href="#start" role="button">Démarrer</a>
      </nav>
    </div>
  </header>

  <main>
    <!-- HERO -->
    <section class="hero">
      <div class="container">
        <h1 class="headline">Décisions <span>48h plus rapides</span>. Deals closés plus vite. ROI x10.</h1>
        <p class="subhead">Velocity = Chief of Staff digital asynchrone pour CEO/COO de scale-ups SaaS (10–50M€ ARR). 2 demandes/jour. Livrables garantis &lt;24h (souvent 1h). Résiliable à tout moment.</p>
        <div class="cta-row">
          <a id="start" class="btn primary" href="https://buy.stripe.com/your-link" target="_blank" rel="noopener">Démarrer maintenant</a>
          <a class="btn" href="#preuves">Voir un exemple (37 min)</a>
        </div>
        <div class="trust">
          <span class="badge">SLA 95 % &lt; 1h (heures ouvrées)</span>
          <span class="badge">Jusqu’à 200h/mois libérées</span>
          <span class="badge">Canal Telegram dédié</span>
        </div>

        <div class="stats container">
          <div class="stat"><h4>2</h4><p>Demandes par jour incluses</p></div>
          <div class="stat"><h4>&lt; 1 h</h4><p>Temps de livraison standard</p></div>
          <div class="stat"><h4>10×</h4><p>ROI visé vs coût mensuel</p></div>
        </div>
      </div>
    </section>

    <!-- OFFRE -->
    <section id="offre">
      <div class="container">
        <h2 class="headline" style="font-size:32px">Offre packagée</h2>
        <p class="muted">Commandes standardisées pour résultats répétés. Tout est livré clair, actionnable, prêt à décider.</p>
        <div class="grid how">
          <div class="card">
            <h3><span class="mono">/brief</span> — Analyse stratégique</h3>
            <p>Note synthèse &lt; 1 h : contexte, 3 insights, 1 recommandation priorisée.</p>
            <span class="badge">Livrable PDF + message Telegram</span>
          </div>
          <div class="card">
            <h3><span class="mono">/decision</span> — Go / No-Go</h3>
            <p>Arbitrage binaire documenté : critères, risques, next step + check-list.</p>
            <span class="badge">Décision en 5 minutes</span>
          </div>
          <div class="card">
            <h3><span class="mono">/compare</span> — Benchmark</h3>
            <p>Tableau fournisseurs/concurrents, scoring, coût total, recommandation.</p>
            <span class="badge">Format table + TL;DR</span>
          </div>
          <div class="card">
            <h3><span class="mono">/deck</span> — Board/Levée</h3>
            <p>Slides prêtes à présenter : KPI, narration, annexes. Export PDF.</p>
            <span class="badge">Mise en page soignée</span>
          </div>
        </div>
      </div>
    </section>

    <!-- PREUVES / DEMOS -->
    <section id="preuves" class="proof">
      <div class="container">
        <h2 class="headline" style="font-size:32px">Preuves & démos</h2>
        <p class="muted">Exemples anonymisés. Temps réels. Impact mesuré.</p>
        <div class="grid">
          <div class="card case">
            <h4>Scale-up SaaS — Partenariat stratégique</h4>
            <div class="kpi">
              <span class="pill">Livré en 37 min</span>
              <span class="pill">Décision +48h</span>
              <span class="pill">+40 k€ MRR sous 3 j</span>
            </div>
            <p style="margin-top:12px" class="muted">/brief → cadrage + /decision → Go → signature 3 jours plus tôt que prévu. Cycle compressé.</p>
          </div>
          <div class="card case">
            <h4>Suite d’outils — Renégociation fournisseur</h4>
            <div class="kpi">
              <span class="pill">Benchmark en 52 min</span>
              <span class="pill">-15 % OPEX</span>
              <span class="pill">ROI x12 / mois</span>
            </div>
            <p style="margin-top:12px" class="muted">/compare → scoring & coûts → renégociation immédiate, économies récurrentes.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- PRICING -->
    <section id="pricing">
      <div class="container">
        <h2 class="headline" style="font-size:32px">Tarif & posture</h2>
        <div class="pricing">
          <div class="card">
            <div class="badge">Abonnement mensuel</div>
            <div class="price">5 000 € / mois</div>
            <ul class="muted" style="list-style:none; padding:0; margin:0">
              <li style="margin:8px 0"><span class="check">✔</span>2 demandes / jour (heures ouvrées)</li>
              <li style="margin:8px 0"><span class="check">✔</span>95 % des livrables &lt; 1 h</li>
              <li style="margin:8px 0"><span class="check">✔</span>Résiliation à tout moment</li>
              <li style="margin:8px 0"><span class="check">✔</span>Canal Telegram dédié</li>
            </ul>
            <div class="cta-row" style="margin-top:16px">
              <a class="btn primary" href="https://buy.stripe.com/your-link" target="_blank" rel="noopener">Démarrer maintenant</a>
              <a class="btn" href="#onboarding">Voir l’onboarding</a>
            </div>
          </div>

          <div class="card">
            <div class="badge">Objectif de valeur</div>
            <p class="muted">Nous visons un **minimum de 50 000 € de valeur business** créée par mois pour 5 000 € investis. Si l’équation n’est pas là, vous arrêtez.</p>
          </div>

          <div class="card">
            <div class="badge">SLA & Qualité</div>
            <p class="muted">Si un livrable dépasse 24 h, la demande est offerte. Zéro friction, zéro réunion inutile, zéro fluff. Juste des décisions plus rapides.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- ONBOARDING -->
    <section id="onboarding">
      <div class="container">
        <h2 class="headline" style="font-size:32px">Onboarding en 4 étapes</h2>
        <div class="steps">
          <div class="card">
            <div class="step-num">1</div>
            <h3>Qualification (15 min)</h3>
            <p class="muted">Cible, usage prévu, priorité. Go/No-Go immédiat.</p>
          </div>
          <div class="card">
            <div class="step-num">2</div>
            <h3>Paiement Stripe</h3>
            <p class="muted">Lien sécurisé. Facture instantanée.</p>
          </div>
          <div class="card">
            <div class="step-num">3</div>
            <h3>Canal Telegram</h3>
            <p class="muted">Ouverture &lt; 24 h. Format et commandes standard partagés.</p>
          </div>
          <div class="card">
            <div class="step-num">4</div>
            <h3>Premier livrable</h3>
            <p class="muted">Le jour même. Vous avez une décision documentée.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- FAQ -->
    <section>
      <div class="container">
        <h2 class="headline" style="font-size:32px">FAQ</h2>
        <div class="grid" style="grid-template-columns: 1fr 1fr;">
          <div class="card">
            <h3>Pourquoi seulement 2 demandes / jour ?</h3>
            <p class="muted">Parce qu’on vise la vitesse et la qualité. Deux demandes bien cadrées suffisent à accélérer votre semaine.</p>
          </div>
          <div class="card">
            <h3>Quid de la confidentialité ?</h3>
            <p class="muted">Canal privé, données minimales, livrables anonymisés publiés uniquement avec votre accord.</p>
          </div>
          <div class="card">
            <h3>Et si je n’ai pas de ROI 10× ?</h3>
            <p class="muted">Vous arrêtez. Pas d’engagement. Notre posture : la valeur parle d’elle‑même.</p>
          </div>
          <div class="card">
            <h3>Puis‑je dépasser 2 demandes ?</h3>
            <p class="muted">Oui, au cas par cas selon charge. L’objectif reste : décision rapide, sans goulot.</p>
          </div>
        </div>
      </div>
    </section>
  </main>

  <footer>
    <div class="container" style="display:flex; align-items:center; justify-content:space-between; gap:10px; flex-wrap: wrap;">
      <div>© <span id="y"></span> VELOCITY. Tous droits réservés.</div>
      <div style="display:flex; gap:12px; align-items:center;">
        <a class="btn" href="https://t.me/your-handle" target="_blank" rel="noopener">Nous écrire sur Telegram</a>
        <a class="btn" href="#offre">Revoir l’offre</a>
      </div>
    </div>
  </footer>

  <script>
    document.getElementById('y').textContent = new Date().getFullYear();
  </script>
</body>
</html>
